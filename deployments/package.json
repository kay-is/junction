{
  "name": "junction",
  "main": "index.ts",
  "scripts": {
    "deploy": "pulumi up && pulumi stack output -j > output.dev.json",
    "destroy": "pulumi destroy"
  },
  "devDependencies": {
    "@types/node": "^20.17.12",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@ao-tools/pulumi-ao": "^0.1.7",
    "@pulumi/pulumi": "^3.113.0"
  }
}
